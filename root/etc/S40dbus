#. /etc/default/rcS

case "$1" in
        start|"")
                mkdir -p /var/run/dbus
                dbus-daemon --system
                ;;
        stop)
                killall dbus-daemon
                ;;
        *)
                echo "Usage: S40dbus {start|stop}" >&2
                exit 1
                ;;
esac
